import { PopoverProps } from '../../interfaces/CommonInterface';
export declare const Popover: ({ content, children, placement, width, height, className, isOpen, setIsOpen }: PopoverProps) => JSX.Element;
